{
  "scripts": {
    "dev": "npm --prefix cropcare-ai run dev",
    "build": "npm --prefix cropcare-ai run build",
    "lint": "npm --prefix cropcare-ai run lint",
    "preview": "npm --prefix cropcare-ai run preview"
  },
  "dependencies": {
    "axios": "^1.19.0"
  }
}
